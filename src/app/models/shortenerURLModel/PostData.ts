import * as models from './models';

export interface PostData {
    createdAt: string;
    domainName:string
    customName: string;
    email: string;
    updatedAt: string;
    url: string;
    urlType: string;
    userType: string;
}
